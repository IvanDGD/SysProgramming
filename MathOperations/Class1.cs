namespace MathOperations
{
    public class ArrayOperations
    {
        public int Sum(int[] array) => array.Sum();
        //public int[] Where(int[] array, Func<int, bool> predicate) => array.Where(predicate)
    }
}
